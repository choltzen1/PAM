[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q -m "not integration"
markers =
	integration: touches external services or real databases
	no_external_writes: guaranteed to avoid external-system writes (uses mocks/in-memory state)